<ul>
<% @groups.each do |group| %>
  <li class="group-result">
    <h3><%= link_to group.name, group_path(group) %></h3>
    <p><%= group.course %></p>
    <p><%= group.size %></p>
  </li>
<% end %>
</ul>
